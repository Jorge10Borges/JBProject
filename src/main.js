import './styles.css'

const app = document.getElementById('app')
app.innerHTML += `
  <div class="mt-4 text-sm text-zinc-500">
    Entorno listo. Edita <code>src/main.js</code> y recarga.
  </div>
`
